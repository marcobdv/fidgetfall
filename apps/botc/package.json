{
  "name": "botc-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "Blood on the Clocktower server for humans and agents",
  "type": "module",
  "workspaces": [
    "packages/engine",
    "packages/server",
    "packages/client"
  ],
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "npm run build --workspace @botc/engine && npm run build --workspace @botc/server",
    "test": "npm run test --workspace @botc/engine && npm run test --workspace @botc/server",
    "start": "npm run start --workspace @botc/server",
    "dev": "npm run build && npm run start",
    "clean": "rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
