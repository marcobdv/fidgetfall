# Pi locally-fetched packages & per-machine trust state (.pi/settings.json is tracked)
.pi/npm/
.pi/git/
.pi/trust.json

# Godot 4 + .NET (per-game artifacts; games also have their own .gitignore)
**/.godot/
**/.mono/
**/bin/
**/obj/
**/build/
**/export/
*.user

# Node (Pi tooling)
node_modules/

# Claude Code per-session local settings (not part of the studio harness)
.claude/settings.local.json

# Machine-specific notes (local toolchain paths etc.)
LOCAL.md

# OS / IDE
.DS_Store
.vs/
.idea/
*.suo

# Per-game event journals (the record, not the repo)
apps/botc/data/journal/
